# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET 	/push/mail             @controllers.Application.putmail
GET 	/push/log              @controllers.Application.putlog
